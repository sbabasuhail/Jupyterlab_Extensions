import * as index from "./tokens/index.js";
import { theme } from "./theme.js";
import { mergeTheme } from "./utils.js";
const makeTheme = (options) => {
  const customTheme = typeof options === "function" ? options(theme) : options;
  const newTheme = mergeTheme(index, customTheme);
  return newTheme;
};
export {
  makeTheme
};
//# sourceMappingURL=makeTheme.js.map

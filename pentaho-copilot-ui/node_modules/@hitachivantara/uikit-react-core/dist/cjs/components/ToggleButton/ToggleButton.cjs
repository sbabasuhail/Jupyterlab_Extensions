"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@emotion/react/jsx-runtime");
const React = require("react");
const useControlled = require("../../hooks/useControlled.cjs");
const useDefaultProps = require("../../hooks/useDefaultProps.cjs");
const Button = require("../Button/Button.cjs");
const HvToggleButton = React.forwardRef((props, ref) => {
  const {
    defaultSelected,
    selected,
    notSelectedIcon,
    selectedIcon = null,
    onClick,
    children,
    ...others
  } = useDefaultProps.useDefaultProps("HvToggleButton", props);
  const [isSelected, setIsSelected] = useControlled.useControlled(selected, Boolean(defaultSelected));
  const onClickHandler = (event) => {
    setIsSelected(!isSelected);
    onClick == null ? void 0 : onClick(event, !isSelected);
  };
  return /* @__PURE__ */ jsxRuntime.jsx(Button.HvButton, { ref, icon: true, "aria-pressed": isSelected, onClick: onClickHandler, ...others, children: children || (!isSelected ? notSelectedIcon : selectedIcon) });
});
exports.HvToggleButton = HvToggleButton;
//# sourceMappingURL=ToggleButton.cjs.map

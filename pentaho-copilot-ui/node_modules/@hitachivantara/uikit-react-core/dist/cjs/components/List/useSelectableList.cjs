"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const React = require("react");
const useSelectableList = (defaultList = []) => {
  const [list, setList] = React.useState(defaultList);
  const selection = React.useMemo(() => list.filter((elem) => elem.selected), [list]);
  return [list, setList, selection];
};
exports.useSelectableList = useSelectableList;
//# sourceMappingURL=useSelectableList.cjs.map

import { jsx } from "@emotion/react/jsx-runtime";
import MuiDialogActions from "@mui/material/DialogActions";
import { useDefaultProps } from "../../../hooks/useDefaultProps.js";
import { useClasses } from "./Actions.styles.js";
import { staticClasses } from "./Actions.styles.js";
const HvDialogActions = (props) => {
  const {
    classes: classesProp,
    className,
    children,
    fullscreen = false,
    ...others
  } = useDefaultProps("HvDialogActions", props);
  const {
    classes,
    cx
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsx(MuiDialogActions, { className, classes: {
    root: cx(classes.root, {
      [classes.fullscreen]: fullscreen
    }),
    spacing: classes.spacing
  }, ...others, children });
};
export {
  HvDialogActions,
  staticClasses as dialogActionClasses
};
//# sourceMappingURL=Actions.js.map

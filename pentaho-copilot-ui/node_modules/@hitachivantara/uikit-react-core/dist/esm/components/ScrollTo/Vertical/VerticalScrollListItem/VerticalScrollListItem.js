import { jsx } from "@emotion/react/jsx-runtime";
import { CurrentStep } from "@hitachivantara/uikit-react-icons";
import { setId } from "../../../../utils/setId.js";
import { useTheme } from "../../../../hooks/useTheme.js";
import { useClasses } from "./VerticalScrollListItem.styles.js";
import { staticClasses } from "./VerticalScrollListItem.styles.js";
const HvVerticalScrollListItem = ({
  id,
  className,
  classes: classesProp,
  selected,
  "aria-label": ariaLabel,
  onClick,
  onKeyDown,
  tooltipWrapper,
  ...others
}) => {
  const {
    classes,
    cx
  } = useClasses(classesProp);
  const {
    activeTheme
  } = useTheme();
  const variant = selected ? "label" : "body";
  const labelId = setId(id, "label");
  const buttonId = setId(id, "button");
  const Tooltip = tooltipWrapper;
  const icon = selected ? /* @__PURE__ */ jsx(CurrentStep, { height: activeTheme == null ? void 0 : activeTheme.scrollTo.dotSelectedSize, width: activeTheme == null ? void 0 : activeTheme.scrollTo.dotSelectedSize }) : /* @__PURE__ */ jsx("div", { className: cx(classes.notSelected) });
  return /* @__PURE__ */ jsx("li", { id, className: cx(classes.root, className), "aria-current": selected, children: /* @__PURE__ */ jsx("div", { id: buttonId, role: "button", tabIndex: 0, onClick, onKeyDown, className: classes.button, "aria-label": ariaLabel, "aria-labelledby": labelId, ...others, children: /* @__PURE__ */ jsx(Tooltip, { id: labelId, className: classes.text, variant, children: icon }) }) });
};
export {
  HvVerticalScrollListItem,
  staticClasses as verticalScrollListItemClasses
};
//# sourceMappingURL=VerticalScrollListItem.js.map

import { jsx } from "@emotion/react/jsx-runtime";
import { useDefaultProps } from "../../hooks/useDefaultProps.js";
import { useClasses } from "./Link.styles.js";
import { staticClasses } from "./Link.styles.js";
const HvLink = (props) => {
  const {
    onClick,
    classes: classesProp,
    className,
    route,
    data,
    children,
    ...others
  } = useDefaultProps("HvLink", props);
  const {
    classes,
    cx
  } = useClasses(classesProp);
  const handleClick = (event) => {
    event.preventDefault();
    onClick == null ? void 0 : onClick(event, data);
  };
  return /* @__PURE__ */ jsx("a", { href: route, onClick: onClick != null ? handleClick : void 0, className: cx(classes.a, className), ...others, children });
};
export {
  HvLink,
  staticClasses as linkClasses
};
//# sourceMappingURL=Link.js.map

import { jsx } from "@emotion/react/jsx-runtime";
import { MoreOptionsHorizontal } from "@hitachivantara/uikit-react-icons";
import { setId } from "../../utils/setId.js";
import { HvDropDownMenu } from "../DropDownMenu/DropDownMenu.js";
const removeExtension = (label) => label.includes(".") ? label.substring(0, label.lastIndexOf(".")) : label;
const pathWithSubMenu = (id, listRoute, maxVisible, onClick, dropDownMenuProps) => {
  const nbrElemToSubMenu = listRoute.length - maxVisible;
  const subMenuList = listRoute.slice(1, nbrElemToSubMenu + 1);
  const handleClick = (event, data) => {
    event.preventDefault();
    onClick == null ? void 0 : onClick(event, data);
  };
  listRoute.splice(1, nbrElemToSubMenu, /* @__PURE__ */ jsx(HvDropDownMenu, { id: setId(id, "submenu"), icon: /* @__PURE__ */ jsx(MoreOptionsHorizontal, { iconSize: "S", color: "secondary" }), dataList: subMenuList, ...dropDownMenuProps, onClick: onClick != null ? handleClick : void 0 }));
  return listRoute;
};
export {
  pathWithSubMenu,
  removeExtension
};
//# sourceMappingURL=utils.js.map

import { jsx } from "@emotion/react/jsx-runtime";
import MuiCardMedia from "@mui/material/CardMedia";
import { useClasses } from "./Media.styles.js";
import { staticClasses } from "./Media.styles.js";
const HvCardMedia = ({
  id,
  classes: classesProp,
  className,
  children,
  title,
  onClick,
  ...others
}) => {
  const {
    classes
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsx(MuiCardMedia, { id, classes: {
    root: classes.root,
    media: classes.media
  }, className, role: "img", title, onClick, ...others, children });
};
export {
  HvCardMedia,
  staticClasses as cardMediaClasses
};
//# sourceMappingURL=Media.js.map

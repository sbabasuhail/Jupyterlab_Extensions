import { jsxs, jsx } from "@emotion/react/jsx-runtime";
import { theme } from "@hitachivantara/uikit-styles";
import { useDefaultProps } from "../../hooks/useDefaultProps.js";
import { useClasses } from "./Card.styles.js";
import { staticClasses } from "./Card.styles.js";
import { HvBox } from "../Box/Box.js";
const HvCard = (props) => {
  const {
    classes: classesProp,
    className,
    children,
    icon,
    selectable = false,
    selected = false,
    statusColor = "sema0",
    bgcolor,
    ...others
  } = useDefaultProps("HvCard", props);
  const {
    classes,
    css,
    cx
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsxs(HvBox, { className: cx("HvIsCardGridElement", classes.root, css({
    backgroundColor: bgcolor && theme.colors[bgcolor] || theme.card.backgroundColor
  }), {
    [classes.selectable]: selectable,
    [classes.selected]: selected
  }, className), ...others, children: [
    /* @__PURE__ */ jsxs("div", { className: classes.semanticContainer, children: [
      /* @__PURE__ */ jsx("div", { className: cx(css({
        height: selected ? 4 : 2,
        backgroundColor: statusColor === "sema0" ? selected ? theme.colors.secondary : theme.colors.atmo4 : theme.colors[statusColor]
      }), classes.semanticBar) }),
      /* @__PURE__ */ jsx("div", { className: classes.icon, children: icon })
    ] }),
    children
  ] });
};
export {
  HvCard,
  staticClasses as cardClasses
};
//# sourceMappingURL=Card.js.map

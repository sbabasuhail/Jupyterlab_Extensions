import { jsx } from "@emotion/react/jsx-runtime";
import { useUniqueId } from "../../../hooks/useUniqueId.js";
import { setId } from "../../../utils/setId.js";
import { useClasses } from "./FileList.styles.js";
import { staticClasses } from "./FileList.styles.js";
import { HvFile } from "../File/File.js";
const HvFileList = ({
  id,
  classes: classesProp,
  list = [],
  removeFileButtonLabel,
  onFileRemoved
}) => {
  const {
    classes
  } = useClasses(classesProp);
  const elementId = useUniqueId(id, "hvfilelist");
  const hasFiles = list.length > 0;
  if (!hasFiles)
    return null;
  return /* @__PURE__ */ jsx("ul", { id: setId(id, "list"), className: classes.list, children: list.map((data) => /* @__PURE__ */ jsx(HvFile, { classes: {
    root: classes == null ? void 0 : classes.listItem
  }, id: setId(elementId, "values"), data, onFileRemoved, removeFileButtonLabel }, data.id)) });
};
export {
  HvFileList,
  staticClasses as fileListClasses
};
//# sourceMappingURL=FileList.js.map

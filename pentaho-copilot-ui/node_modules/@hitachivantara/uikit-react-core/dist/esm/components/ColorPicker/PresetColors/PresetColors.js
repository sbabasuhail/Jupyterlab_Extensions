import { jsxs, jsx } from "@emotion/react/jsx-runtime";
import { Swatch } from "react-color/lib/components/common";
import { useClasses } from "./PresetColors.styles.js";
import { staticClasses } from "./PresetColors.styles.js";
import { HvTypography } from "../../Typography/Typography.js";
const PresetColors = ({
  onClick,
  colors,
  title,
  className,
  classes: classesProp
}) => {
  const {
    classes,
    cx
  } = useClasses(classesProp);
  const handleClick = (hex) => {
    onClick({
      hex,
      source: "hex"
    });
  };
  return /* @__PURE__ */ jsxs("div", { className: cx(classes.root, className), children: [
    title && /* @__PURE__ */ jsx(HvTypography, { className: classes.title, variant: "caption1", children: title }),
    /* @__PURE__ */ jsx("div", { className: classes.colors, children: colors.map((color, index) => /* @__PURE__ */ jsx("div", { className: classes.swatchWrap, children: /* @__PURE__ */ jsx(Swatch, { color, onClick: handleClick, focusStyle: {
      boxShadow: `inset 0 0 0 1px rgba(0,0,0,.15), 0 0 4px ${color}`
    } }) }, `recommended-color-${color}-${index}`)) })
  ] });
};
export {
  PresetColors,
  staticClasses as colorPickerPresetColorsClasses
};
//# sourceMappingURL=PresetColors.js.map

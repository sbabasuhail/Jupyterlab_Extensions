import { jsx, jsxs, Fragment } from "@emotion/react/jsx-runtime";
import { useCallback } from "react";
import { Saturation, Hue } from "react-color/lib/components/common";
import { CustomPicker } from "react-color";
import { useTheme } from "../../../hooks/useTheme.js";
import { useClasses } from "./Picker.styles.js";
import { staticClasses } from "./Picker.styles.js";
import { Fields } from "../Fields/Fields.js";
import { HvTypography } from "../../Typography/Typography.js";
const Picker = CustomPicker(({
  onChange,
  rgb,
  hsl,
  hsv,
  hex,
  title,
  classes: classesProp
}) => {
  const {
    activeTheme
  } = useTheme();
  const {
    classes
  } = useClasses(classesProp);
  const SaturationPointer = useCallback(() => /* @__PURE__ */ jsx("div", { className: classes == null ? void 0 : classes.saturationPointer }), [classes == null ? void 0 : classes.saturationPointer]);
  const HueSlider = useCallback(() => /* @__PURE__ */ jsx("div", { className: classes == null ? void 0 : classes.hueSlider }), [classes == null ? void 0 : classes.hueSlider]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    title && /* @__PURE__ */ jsx(HvTypography, { className: classes.title, variant: "caption1", children: title }),
    /* @__PURE__ */ jsxs("div", { className: classes.pickers, children: [
      /* @__PURE__ */ jsx("div", { className: classes.saturation, children: /* @__PURE__ */ jsx(Saturation, { hsl, hsv, onChange, pointer: SaturationPointer }) }),
      /* @__PURE__ */ jsx("div", { className: classes.hue, children: /* @__PURE__ */ jsx(Hue, { direction: activeTheme == null ? void 0 : activeTheme.colorPicker.hueDirection, hsl, onChange, pointer: HueSlider }) })
    ] }),
    /* @__PURE__ */ jsx(Fields, { className: classes.fields, rgb, hex, onChange })
  ] });
});
export {
  Picker,
  staticClasses as colorPickerPickerClasses
};
//# sourceMappingURL=Picker.js.map

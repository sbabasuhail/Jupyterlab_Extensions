import { jsx } from "@emotion/react/jsx-runtime";
import { forwardRef } from "react";
import { useClasses } from "./Carousel.styles.js";
import { HvStack } from "../Stack/Stack.js";
import { HvButton } from "../Button/Button.js";
const HvCarouselThumbnails = forwardRef((props, ref) => {
  const {
    classes: classesProp,
    className,
    page,
    pages,
    width,
    renderThumbnail,
    onThumbnailClick,
    thumbnailProps,
    ...others
  } = props;
  const {
    classes,
    cx
  } = useClasses(classesProp);
  const selectedIndex = page || 0;
  const numSlides = pages;
  return /* @__PURE__ */ jsx("div", { ref, className: cx(classes.panel, className), ...others, children: /* @__PURE__ */ jsx(HvStack, { direction: "row", spacing: "xs", children: Array.from(Array(numSlides)).map((doc, i) => /* @__PURE__ */ jsx(HvButton, { icon: true, style: {
    width
  }, variant: "secondaryGhost", className: cx(classes.thumbnail, {
    [classes.thumbnailSelected]: i === selectedIndex
  }), onClick: (event) => onThumbnailClick == null ? void 0 : onThumbnailClick(event, i), ...thumbnailProps, children: renderThumbnail == null ? void 0 : renderThumbnail(i) }, `thumbnail-${i}`)) }) });
});
export {
  HvCarouselThumbnails
};
//# sourceMappingURL=CarouselThumbnails.js.map

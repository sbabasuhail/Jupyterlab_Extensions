import { jsx } from "@emotion/react/jsx-runtime";
import { useDefaultProps } from "../../hooks/useDefaultProps.js";
import React__default, { cloneElement } from "react";
import { useClasses } from "./MultiButton.styles.js";
import { staticClasses } from "./MultiButton.styles.js";
const HvMultiButton = (props) => {
  const {
    className,
    children,
    classes: classesProp,
    disabled = false,
    vertical = false,
    variant = "secondarySubtle",
    ...others
  } = useDefaultProps("HvMultiButton", props);
  const {
    classes,
    cx
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsx("div", { className: cx(classes.root, {
    [classes.vertical]: vertical
  }, className), ...others, children: React__default.Children.map(children, (child) => {
    if (React__default.isValidElement(child)) {
      const childIsSelected = !!child.props.selected;
      return cloneElement(child, {
        variant,
        disabled: disabled || child.props.disabled,
        className: cx(child.props.className, classes.button, {
          [classes.selected]: childIsSelected
        }),
        "aria-pressed": childIsSelected
      });
    }
  }) });
};
export {
  HvMultiButton,
  staticClasses as multiButtonClasses
};
//# sourceMappingURL=MultiButton.js.map

import { jsx } from "@emotion/react/jsx-runtime";
import { useDefaultProps } from "../../hooks/useDefaultProps.js";
import { useClasses } from "./Login.styles.js";
import { staticClasses } from "./Login.styles.js";
const HvLogin = (props) => {
  const {
    id,
    className,
    classes: classesProp,
    children,
    background,
    ...others
  } = useDefaultProps("HvLogin", props);
  const {
    classes,
    cx
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsx("div", { id, className: cx(classes.root, className), style: {
    backgroundImage: background && `url(${background})`
  }, ...others, children: /* @__PURE__ */ jsx("div", { className: classes.formContainer, children }) });
};
export {
  HvLogin,
  staticClasses as loginClasses
};
//# sourceMappingURL=Login.js.map

import { jsx } from "@emotion/react/jsx-runtime";
import { useDefaultProps } from "../../hooks/useDefaultProps.js";
import { useClasses } from "./Header.styles.js";
import { staticClasses } from "./Header.styles.js";
const HvHeader = (props) => {
  const {
    className,
    classes: classesProp,
    children,
    position = "fixed",
    ...others
  } = useDefaultProps("HvHeader", props);
  const {
    classes,
    cx,
    css
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsx("header", { className: cx(classes.root, classes.backgroundColor, css({
    position,
    ...position === "fixed" && {
      top: 0,
      left: "auto",
      right: 0
    }
  }), className), ...others, children: /* @__PURE__ */ jsx("div", { className: classes.header, children }) });
};
export {
  HvHeader,
  staticClasses as headerClasses
};
//# sourceMappingURL=Header.js.map

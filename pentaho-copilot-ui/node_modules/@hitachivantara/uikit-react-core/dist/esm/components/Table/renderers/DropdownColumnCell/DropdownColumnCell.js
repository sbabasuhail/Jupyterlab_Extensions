import { jsx } from "@emotion/react/jsx-runtime";
import { HvDropdown } from "../../../Dropdown/Dropdown.js";
const HvDropdownColumnCell = ({
  values,
  disabled,
  onChange,
  placeholder,
  dropdownProps
}) => {
  return /* @__PURE__ */ jsx(HvDropdown, { onChange: (item) => {
    onChange == null ? void 0 : onChange(item);
  }, placeholder, disabled, values, disablePortal: true, ...dropdownProps });
};
export {
  HvDropdownColumnCell
};
//# sourceMappingURL=DropdownColumnCell.js.map

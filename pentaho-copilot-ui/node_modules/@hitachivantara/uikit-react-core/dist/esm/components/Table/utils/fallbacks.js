const LONG_DASH = "—";
const hvStringFallback = (value) => {
  return typeof value === "string" && value !== "" ? value : LONG_DASH;
};
const hvNumberFallback = (value) => {
  return typeof value === "number" ? value : LONG_DASH;
};
const hvNodeFallback = (value) => {
  if (!value)
    return LONG_DASH;
  return hvStringFallback(value == null ? void 0 : value.toString()) === LONG_DASH ? LONG_DASH : value;
};
export {
  hvNodeFallback,
  hvNumberFallback,
  hvStringFallback
};
//# sourceMappingURL=fallbacks.js.map

import { jsxs, Fragment, jsx } from "@emotion/react/jsx-runtime";
import { useClasses } from "./CalendarWeekLabels.styles.js";
import { staticClasses } from "./CalendarWeekLabels.styles.js";
import { HvTypography } from "../../Typography/Typography.js";
const HvCalendarWeekLabel = ({
  classes: classesProp,
  labels = []
}) => {
  const {
    classes
  } = useClasses(classesProp);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    " ",
    labels.map((dayName, index) => {
      const key = `${dayName}-${index}`;
      return /* @__PURE__ */ jsx(HvTypography, { variant: "label", className: classes.calendarDay, children: dayName }, key);
    }),
    " "
  ] });
};
export {
  HvCalendarWeekLabel,
  staticClasses as calendarWeekLabelsClasses
};
//# sourceMappingURL=CalendarWeekLabels.js.map

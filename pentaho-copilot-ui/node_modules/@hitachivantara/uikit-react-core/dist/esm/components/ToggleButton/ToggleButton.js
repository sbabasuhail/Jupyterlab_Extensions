import { jsx } from "@emotion/react/jsx-runtime";
import { forwardRef } from "react";
import { useControlled } from "../../hooks/useControlled.js";
import { useDefaultProps } from "../../hooks/useDefaultProps.js";
import { HvButton } from "../Button/Button.js";
const HvToggleButton = forwardRef((props, ref) => {
  const {
    defaultSelected,
    selected,
    notSelectedIcon,
    selectedIcon = null,
    onClick,
    children,
    ...others
  } = useDefaultProps("HvToggleButton", props);
  const [isSelected, setIsSelected] = useControlled(selected, Boolean(defaultSelected));
  const onClickHandler = (event) => {
    setIsSelected(!isSelected);
    onClick == null ? void 0 : onClick(event, !isSelected);
  };
  return /* @__PURE__ */ jsx(HvButton, { ref, icon: true, "aria-pressed": isSelected, onClick: onClickHandler, ...others, children: children || (!isSelected ? notSelectedIcon : selectedIcon) });
});
export {
  HvToggleButton
};
//# sourceMappingURL=ToggleButton.js.map

import { jsx } from "@emotion/react/jsx-runtime";
import { theme } from "@hitachivantara/uikit-styles";
import { getIconSize, IconBase } from "./IconBase.js";
import { isXS, isSemantic, isSelector, isSort } from "./utils.js";
const getSecondaryColor = (iconName) => {
  if (isSelector(iconName))
    return theme.colors.atmo1;
  if (isSort(iconName))
    return theme.colors.atmo4;
  return theme.colors.atmo2;
};
const HvIconSprite = ({
  spriteUrl,
  iconName,
  color,
  iconSize: iconSizeProp,
  height,
  width,
  svgProps = {},
  ...others
}) => {
  const iconSize = iconSizeProp ?? (isXS(iconName) ? "XS" : "S");
  const baseColors = [theme.colors.secondary, getSecondaryColor(iconName)];
  const size = getIconSize(iconSize, isSemantic(iconName), width, height);
  return /* @__PURE__ */ jsx(IconBase, { iconName, iconSize, color, palette: baseColors, ...others, children: /* @__PURE__ */ jsx("svg", { xmlns: "http://www.w3.org/2000/svg", height: size.height, width: size.width, focusable: false, ...svgProps, children: /* @__PURE__ */ jsx("use", { href: `${spriteUrl}#${iconName}`, height: size.height, width: size.width }) }) });
};
export {
  HvIconSprite
};
//# sourceMappingURL=IconSprite.js.map

import { useTheme as h } from "@hitachivantara/uikit-react-core";
import m, { LOCAL_STORAGE_KEYS as a } from "./useLocalStorage.js";
const i = () => {
  const {
    selectedTheme: s,
    changeTheme: d,
    selectedMode: r,
    colorModes: e
  } = h(), {
    setStoredValue: c
  } = m(a.COLOR_MODE), l = () => {
    const t = (e.indexOf(r) + 1) % e.length;
    return e[t];
  };
  return {
    handleThemeEvent: (o) => {
      const {
        colorMode: t
      } = o.detail, n = e.indexOf(t) !== -1 ? t : l();
      d(s, n), c(n);
    }
  };
}, E = i;
export {
  E as default
};
//# sourceMappingURL=useThemeEventListener.js.map

import { useHvSnackbar as l } from "@hitachivantara/uikit-react-core";
import i from "../../providers/hooks/useBannerContext.js";
import t from "../types/notifications.js";
const d = () => {
  const {
    enqueueSnackbar: n
  } = l(), {
    show: o
  } = i(), r = (e) => {
    const a = e.actions ? {
      action: Array.isArray(e.actions) ? e.actions[0] : e.actions,
      actionCallback: e.actionsCallback
    } : void 0;
    e.message && n(e.message, {
      variant: e.variant || "default",
      snackbarContentProps: a
    });
  }, c = (e) => {
    e.message && o(e);
  };
  return {
    handleNotificationEvent: (e) => {
      var s;
      const a = (s = e.detail) == null ? void 0 : s.type.toUpperCase();
      a === t.SNACKBAR ? r(e.detail) : a === t.BANNER ? c(e.detail) : console.warn("Invalid notification type", a);
    }
  };
}, C = d;
export {
  C as default
};
//# sourceMappingURL=useNotificationsEventListener.js.map
